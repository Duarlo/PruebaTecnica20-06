package persona;

public class datospersonas extends persona{
	
	public class void main(args String[]) {
		
		String datos= "jhon-Hotmail-22/04/1997-doe-H+katty-Gmail-05/08/2002-taylor-M+james-Yahoo-14/02/2005-green-H+allison-Hotmail-25/12/1980-parker-M";
		Persona persona =new Persona();
		
			int i=0;
			do {
			do {
				setNombreCompleto=datos.charAt(i).concat(setNombreCompleto);
				i++;
			}
			while(datos.charAt(i)!='-');
			do {
				setCorreo=datos.charAt(i).concat(setCorreo);
				i++;
			}
			while(datos.charAt(i)!='-');
			do {
				setDate=datos.charAt(i).concat(setDate);
				i++;
			}
			while(datos.charAt(i)!='-');
			do {
				setNombrecompleto=datos.charAt(i).concat(setNombreCompleto);
				i++;
			}
			while(datos.charAt(i)!='-');
			do {
				setGenero=datos.charAt(i).concat(setGenero);
				i++;
			}
			while(datos.charAt(i)!='-');
			}
			while(datos.charAt(i)!='+');
			
			setNombrecompleto=datos.
		}
	
	}
	
	
	
	
	
	
}
